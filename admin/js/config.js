/* Velocube Support Panel — configuration
   Paste your Supabase project credentials here to go live.
   Leave both empty to run in Demo Mode (built-in demo agent and data).

   Where to find these: Supabase Dashboard -> Project Settings -> API.
   The anon key is safe to ship in front-end code; row-level security
   (see supabase-setup.sql) is what protects the data. */
window.VELO_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
